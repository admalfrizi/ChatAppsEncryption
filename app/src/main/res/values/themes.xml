<resources>
    <!-- Base application theme. -->
    <style name="Theme.ChatAppsTrials" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" >?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="textActive">
        <item name="android:textColor">@color/selected</item>
        <item name="android:textSize">12sp</item>
    </style>

    <style name="textInActive">
        <item name="android:textColor">@color/unselected</item>
        <item name="android:textSize">12sp</item>
    </style>

    <style name="Theme.CustomSplashScreenTheme" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_round</item>
        <item name="windowSplashScreenAnimationDuration">300</item>
        <item name="splashScreenIconSize">100dp</item>
        <item name="postSplashScreenTheme">@style/Theme.ChatAppsTrials</item>
    </style>
</resources>